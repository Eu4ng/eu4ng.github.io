---
layout: post
title: 쿠버네티스 서비스를 VPN 없이 외부에서 HTTPS로 접속하는 방법
description: Cloudflare는 DNS와 프록시로만 쓰고 cert-manager, Traefik, DDNS, UPnP 포트포워딩은 GitOps 저장소와 스크립트로 관리해, 홈랩 쿠버네티스의 웹 서비스를 도메인으로 여는 방법을 정리했습니다.
author: Eu4ng
tags: [kubernetes, traefik, cert-manager, cloudflare, lets-encrypt, ddns, upnp, argo-cd, gitops]
permalink: /posts/40/
---

회사망처럼 VPN이 막힌 곳이나 클라이언트를 설치할 수 없는 남의 기기에서도 브라우저만으로 클러스터의 웹 서비스에 접속하도록 `https://[서비스].[DOMAIN]` 주소를 엽니다. Cloudflare Tunnel과 Access(Zero Trust)를 쓰면 더 간단하지만 라우팅 규칙과 접근 정책이 대시보드에만 남으므로, Cloudflare는 DNS와 프록시(집 IP 숨김)로만 쓰고 인증서 발급, DNS 갱신, 라우팅, 포트포워딩은 전부 GitOps 저장소와 스크립트에 둡니다. 서비스를 하나 더 열 때 하는 일은 Ingress 파일 하나와 DDNS 목록에 이름 한 줄을 추가하는 것뿐입니다.

```text
브라우저 ─HTTPS→ Cloudflare 프록시 ─HTTPS→ 공유기 443 ─포워딩→ Traefik NodePort(31443) ─Ingress→ 각 Service
                    ▲ [서비스].[DOMAIN] A 레코드                    ▲ Let's Encrypt 와일드카드 인증서
                    └─ cloudflare-ddns 가 공인 IP 로 갱신             └─ cert-manager 가 DNS-01 로 발급, Traefik 이 기본 인증서로 사용
```

1. Cloudflare API 토큰 만들기
2. Cloudflare 설정 스크립트 실행
3. GitOps 저장소에 cert-manager, Traefik, DDNS 추가
4. 공유기 포트포워딩
5. 서비스 노출
6. 접속 확인

## 사전 준비

> 이미 준비되어 있는 경우 건너뛰셔도 됩니다.
{: .prompt-info }

아래 환경을 기준으로 작성했습니다.

| 항목 | 버전 |
| :--- | :--- |
| Kubernetes | `v1.37` |
| Argo CD | `v3.5` |
| cert-manager | `v1.21.2` |
| Traefik Helm 차트 | `41.6.0` |
| cloudflare-ddns | `1.17.1` |
| 공유기 | `ipTIME T5008SE` (UPnP 켬) |
| 작성 기준일 | `2026-09-22` |

다음 항목이 준비되어 있어야 합니다.

- Argo CD가 `services/[이름]/` 폴더를 Application으로 배포하는 GitOps 저장소 ([쿠버네티스에 Argo CD 설치하고 GitOps로 서비스 추가하는 방법](/posts/36/) 참고)
- Cloudflare 계정에 추가되어 네임서버가 Cloudflare로 바뀐 도메인 (스크립트가 존을 대신 추가해 주지만, 네임서버 변경은 등록기관에서 직접 해야 합니다)
- 공유기가 공인 IP를 직접 받는 환경 (공유기 WAN 주소가 `100.64.0.0/10` 대역이면 통신사 NAT 뒤라 이 방법을 쓸 수 없습니다)
- 컨트롤플레인에 SSH로 접속해 `kubectl`을 쓸 수 있는 PC

## 1. Cloudflare API 토큰 만들기

설정 스크립트가 존을 만들고 SSL 모드를 바꾸고 클러스터용 토큰을 발급하는 데 쓸 토큰입니다. 이 토큰은 저장소나 클러스터에 넣지 않고 스크립트 실행 때만 입력합니다.

1. Cloudflare 대시보드에서 **Manage Account** > **Account API Tokens** > **Create Token** > **Custom Token**
2. 권한 네 개를 추가
   - **Account** > **Account API Tokens** > **Edit**
   - **Zone** > **Zone** > **Edit**
   - **Zone** > **Zone Settings** > **Edit**
   - **Zone** > **Zone** > **Read**
3. **Account Resources**는 본인 계정, **Zone Resources**는 **All zones from an account**
4. **Continue to summary** > **Create Token** 후 값을 복사 (다시 볼 수 없습니다)

> 클러스터용 토큰은 사용자 소유가 아니라 계정 소유 토큰(`cfat_`로 시작)으로 발급합니다. 사용자 소유 토큰을 API로 만드는 데 필요한 **User** > **API Tokens** > **Edit** 권한은 Custom Token 화면에 없기 때문입니다. 계정 소유 토큰은 cert-manager와 cloudflare-ddns 1.17 이상에서 그대로 동작합니다.
{: .prompt-warning }

- **확인:** 토큰 검토 화면의 권한 정책에 `Account API Tokens Write`, `Zone Write`, `Zone Settings Write`, `Zone Read` 네 줄이 보입니다.

## 2. Cloudflare 설정 스크립트 실행

컨트롤플레인에 SSH가 되는 PC에서 실행합니다. 존이 없으면 추가하고 등록기관에 넣을 네임서버를 알려 주며, SSL 모드를 **Full (strict)**로 바꾸고, DNS 편집만 되는 클러스터용 토큰을 발급해 `cert-manager`와 `cloudflare-ddns` 네임스페이스에 Secret `cloudflare-api-token`으로 넣습니다. 여러 번 실행해도 결과가 같습니다.

```bash
# 스크립트 내려받기
curl -fsSLO https://eu4ng.github.io/assets/scripts/kubernetes/cloudflare-setup.sh

# 상단의 CONTROL_PLANE 을 컨트롤플레인 주소로 바꾼 뒤 실행 (토큰은 실행 중에 입력)
bash cloudflare-setup.sh [DOMAIN]
```

<details markdown="1">
<summary>cloudflare-setup.sh 전문</summary>

```bash
#!/usr/bin/env bash
#
# 외부 노출에 필요한 Cloudflare 쪽 설정을 API 로 합니다. 여러 번 실행해도 결과가 같습니다.
#   1. 존(도메인) 추가 → 등록기관에 넣을 네임서버 출력
#   2. SSL 모드 Full (strict), 최소 TLS 1.2
#   3. 클러스터용 토큰(DNS 편집만 가능) 발급 → cert-manager·cloudflare-ddns 네임스페이스에 Secret 생성
#
# 사용법: bash cloudflare-setup.sh [DOMAIN]
# 준비:   설정용 API 토큰(사용자 소유·계정 소유 어느 쪽이든)을 실행 중 입력합니다.
#         권한: Account/Account API Tokens/Edit, Zone/Zone/Edit, Zone/Zone Settings/Edit, Zone/Zone/Read,
#         Zone Resources 는 "All zones from an account". 이 토큰은 저장소·클러스터 어디에도 넣지 않습니다.
# 필요:   curl, python3, ssh(컨트롤플레인 kubectl)

set -euo pipefail

# ---------- 환경에 맞게 수정 ----------
CONTROL_PLANE=ubuntu@[CONTROL_PLANE_IP]  # kubectl 을 실행할 곳
SECRET_NAMESPACES="cert-manager cloudflare-ddns"
RUNTIME_TOKEN_NAME=k8s-gitops-dns        # 클러스터에 들어갈 토큰 이름 (Cloudflare 대시보드에서 보입니다)
# --------------------------------------

API=https://api.cloudflare.com/client/v4

log() { echo -e "\n\033[1;32m==>\033[0m $*"; }
die() { echo -e "\033[1;31m[오류]\033[0m $*" >&2; exit 1; }
trap 'echo -e "\033[1;31m[오류]\033[0m ${LINENO}번째 줄에서 중단되었습니다." >&2' ERR

# ---------- 1. 사전 검사 ----------
DOMAIN=${1:-}
[[ "$DOMAIN" =~ ^[a-z0-9.-]+\.[a-z]+$ ]] || die "사용법: bash cloudflare-setup.sh [DOMAIN]"
for cmd in curl python3 ssh; do command -v "$cmd" >/dev/null || die "$cmd 가 필요합니다."; done
read -rs -p "Cloudflare 설정용 API 토큰: " CLOUDFLARE_API_TOKEN; echo
[ -n "$CLOUDFLARE_API_TOKEN" ] || die "토큰이 비어 있습니다."

# cf METHOD PATH [JSON]  → 응답 JSON 을 표준출력으로. success=false 면 오류 메시지를 보이고 실패합니다.
# 호출 결과는 반드시 `resp=$(cf …)` 처럼 단독 할당으로 받습니다. 그래야 set -e 가 실패를 잡습니다.
cf() {
  local method=$1 path=$2 body=${3:-} out
  if [ -n "$body" ]; then
    out=$(curl -sS -X "$method" "$API$path" -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
          -H "Content-Type: application/json" --data "$body")
  else
    out=$(curl -sS -X "$method" "$API$path" -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN")
  fi
  python3 -c '
import json, sys
raw = sys.stdin.read()
try:
    r = json.loads(raw)
except ValueError:
    sys.exit("응답이 JSON 이 아닙니다: " + raw[:200])
if not r.get("success"):
    sys.exit("API 오류 %s %s: %s" % (sys.argv[1], sys.argv[2], "; ".join("%s %s" % (e.get("code"), e.get("message")) for e in r.get("errors", []))))
print(raw)' "$method" "$path" <<<"$out"
}

# jget JSON 파이썬식 → 예: jget "$zone" 'r["result"]["id"]'
jget() { python3 -c 'import json, sys; r = json.loads(sys.argv[1]); v = eval(sys.argv[2]); print(v if isinstance(v, str) else json.dumps(v, ensure_ascii=False))' "$1" "$2"; }

# ---------- 2. 토큰 검증 ----------
log "설정용 토큰 검증"
resp=$(cf GET '/accounts?per_page=1')
account_id=$(jget "$resp" 'r["result"][0]["id"]')
# 계정 소유 토큰(cfat_…)과 사용자 소유 토큰은 검증 주소가 다릅니다.
if [[ "$CLOUDFLARE_API_TOKEN" == cfat_* ]]; then
  resp=$(cf GET "/accounts/$account_id/tokens/verify")
else
  resp=$(cf GET /user/tokens/verify)
fi
status=$(jget "$resp" 'r["result"]["status"]')
[ "$status" = active ] || die "토큰 상태가 $status 입니다."
echo "계정: $account_id"

# ---------- 3. 존 ----------
log "존 $DOMAIN"
zones=$(cf GET "/zones?name=$DOMAIN")
if [ "$(jget "$zones" 'len(r["result"])')" = 0 ]; then
  echo "존이 없어 추가합니다."
  resp=$(cf POST /zones "{\"name\":\"$DOMAIN\",\"account\":{\"id\":\"$account_id\"},\"type\":\"full\"}")
  zone=$(jget "$resp" 'r["result"]')
else
  echo "존이 이미 있습니다."
  zone=$(jget "$zones" 'r["result"][0]')
fi
zone_id=$(jget "$zone" 'r["id"]')
zone_status=$(jget "$zone" 'r["status"]')
name_servers=$(jget "$zone" '" ".join(r["name_servers"])')
echo "존 ID: $zone_id, 상태: $zone_status"

# ---------- 4. 존 설정 ----------
log "SSL 모드 Full (strict), 최소 TLS 1.2"
cf PATCH "/zones/$zone_id/settings/ssl" '{"value":"strict"}' >/dev/null
cf PATCH "/zones/$zone_id/settings/min_tls_version" '{"value":"1.2"}' >/dev/null
echo "적용했습니다. (HTTP→HTTPS 리다이렉트는 Traefik 이 하므로 Always Use HTTPS 는 켜지 않습니다)"

# ---------- 5. 클러스터용 토큰 ----------
# DNS 편집과 존 읽기만 되는 계정 소유 토큰(cfat_…)을 이 존에 한해 만듭니다. 사용자 소유 토큰을 만드는 데 필요한
# User/API Tokens/Edit 권한은 Custom 토큰 빌더에 없기 때문입니다. 값은 생성 때만 볼 수 있으므로,
# 클러스터에 Secret 이 이미 다 있으면 건너뛰고, 하나라도 없으면 같은 이름의 옛 토큰을 지우고 새로 만듭니다.
log "클러스터용 토큰 $RUNTIME_TOKEN_NAME"
missing=$(ssh "$CONTROL_PLANE" "for ns in $SECRET_NAMESPACES; do kubectl -n \$ns get secret cloudflare-api-token >/dev/null 2>&1 || echo \$ns; done")
resp=$(cf GET "/accounts/$account_id/tokens?per_page=50")
existing_ids=$(jget "$resp" "\" \".join(t[\"id\"] for t in r[\"result\"] if t[\"name\"] == \"$RUNTIME_TOKEN_NAME\")")
if [ -z "$missing" ] && [ -n "$existing_ids" ]; then
  echo "토큰과 Secret 이 모두 있어 건너뜁니다."
else
  for id in $existing_ids; do
    echo "옛 토큰 $id 삭제"
    cf DELETE "/accounts/$account_id/tokens/$id" >/dev/null
  done
  groups=$(cf GET "/accounts/$account_id/tokens/permission_groups")
  dns_write=$(jget "$groups" '[g["id"] for g in r["result"] if g["name"] == "DNS Write"][0]')
  zone_read=$(jget "$groups" '[g["id"] for g in r["result"] if g["name"] == "Zone Read"][0]')
  resp=$(cf POST "/accounts/$account_id/tokens" "{
    \"name\": \"$RUNTIME_TOKEN_NAME\",
    \"policies\": [{
      \"effect\": \"allow\",
      \"resources\": {\"com.cloudflare.api.account.zone.$zone_id\": \"*\"},
      \"permission_groups\": [{\"id\": \"$dns_write\"}, {\"id\": \"$zone_read\"}]
    }]
  }")
  runtime_token=$(jget "$resp" 'r["result"]["value"]')
  echo "발급했습니다."

  # ---------- 6. 클러스터 Secret ----------
  # 토큰은 표준입력으로 넘겨 명령줄·셸 히스토리에 남기지 않습니다.
  log "클러스터 Secret 생성 ($SECRET_NAMESPACES)"
  for ns in $SECRET_NAMESPACES; do
    printf '%s' "$runtime_token" | ssh "$CONTROL_PLANE" "
      kubectl get ns $ns >/dev/null 2>&1 || kubectl create ns $ns >/dev/null
      kubectl -n $ns create secret generic cloudflare-api-token --from-file=token=/dev/stdin --dry-run=client -o yaml \
        | kubectl apply -f -"
  done
fi

# ---------- 7. 요약 ----------
log "완료"
if [ "$zone_status" = active ]; then
  echo "존이 활성 상태입니다. 네임서버는 이미 Cloudflare 를 가리킵니다."
else
  echo "존 상태가 '$zone_status' 입니다. 등록기관에서 네임서버를 아래 둘로 바꾸세요 (전파까지 수 분~수 시간):"
  for ns in $name_servers; do echo "  $ns"; done
fi
```
{: file="cloudflare-setup.sh" }

</details>

- **확인:** 마지막에 `존이 활성 상태입니다` 또는 네임서버 두 개가 출력되고, 컨트롤플레인에서 `kubectl -n cert-manager get secret cloudflare-api-token`과 `kubectl -n cloudflare-ddns get secret cloudflare-api-token`이 각각 Secret을 보여 줍니다.

## 3. GitOps 저장소에 cert-manager, Traefik, DDNS 추가

세 폴더를 추가하고 push 하면 Argo CD가 순서 없이 올립니다. cert-manager의 ClusterIssuer는 CRD가 먼저 있어야 하므로 첫 sync가 한 번 실패한 뒤 재시도로 붙습니다.

> Helm 폴더(`Chart.yaml`이 있는 폴더)에 일반 매니페스트를 함께 두려면 `templates/` 아래에 넣습니다. 폴더 바로 아래의 `*.yaml`은 Helm이 읽지 않아 조용히 무시됩니다.
{: .prompt-warning }

cert-manager는 Let's Encrypt에 DNS-01로 소유권을 증명합니다. 80 포트를 열지 않아도 되고 와일드카드 인증서가 됩니다.

```yaml
apiVersion: v2
name: cert-manager
version: 0.1.0
dependencies:
  - name: cert-manager
    version: v1.21.2
    repository: https://charts.jetstack.io
```
{: file="services/cert-manager/Chart.yaml" }

```yaml
cert-manager:
  crds: { enabled: true }   # CRD 를 차트와 함께 관리합니다
```
{: file="services/cert-manager/values.yaml" }

```yaml
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef: { name: letsencrypt-account }
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef: { name: cloudflare-api-token, key: token }
```
{: file="services/cert-manager/templates/clusterissuer.yaml" }

Traefik은 NodePort로 받습니다. DaemonSet과 `externalTrafficPolicy: Local`로 접속자 IP를 보존해야 뒤의 Cloudflare 대역 판별이 됩니다. Cloudflare 대역은 [공식 목록](https://www.cloudflare.com/ips-v4)을 그대로 옮기고, 서브차트 값에 낯선 키를 넣으면 차트 스키마가 거부하므로 상위 차트 값(루트)에 둡니다.

```yaml
# https://www.cloudflare.com/ips-v4 — 접속자 IP 신뢰(forwardedHeaders)와 그 외 차단(templates/middleware.yaml)에 같이 씁니다.
cloudflareIPv4: &cloudflare-ipv4
  - 173.245.48.0/20
  - 103.21.244.0/22
  - 103.22.200.0/22
  - 103.31.4.0/22
  - 141.101.64.0/18
  - 108.162.192.0/18
  - 190.93.240.0/20
  - 188.114.96.0/20
  - 197.234.240.0/22
  - 198.41.128.0/17
  - 162.158.0.0/15
  - 104.16.0.0/13
  - 104.24.0.0/14
  - 172.64.0.0/13
  - 131.0.72.0/22

traefik:
  deployment:
    kind: DaemonSet                              # 두 노드 모두에서 받습니다. 공유기는 어느 노드 IP 로 넘겨도 됩니다
  tolerations:
    - { key: node-role.kubernetes.io/control-plane, operator: Exists, effect: NoSchedule }
  service:
    spec:
      type: NodePort                             # 외부 IP 를 줄 로드밸런서가 없습니다
      externalTrafficPolicy: Local               # 접속자 IP 를 SNAT 없이 보존해야 Cloudflare 대역 판별이 됩니다
  ports:
    web:
      nodePort: 31080                            # 공유기: WAN 80 → [NODE_IP]:31080
      http:
        redirections:
          entryPoint: { to: websecure, scheme: https, permanent: true }
    websecure:
      nodePort: 31443                            # 공유기: WAN 443 → [NODE_IP]:31443
      forwardedHeaders:
        trustedIPs: *cloudflare-ipv4              # 실제 접속자 IP(X-Forwarded-For)를 신뢰합니다
      http:
        middlewares: [traefik-cloudflare-only@kubernetescrd]   # 프록시를 거치지 않고 집 IP 로 직접 오는 요청은 403
  providers:
    kubernetesIngress:
      publishedService: { enabled: false }       # NodePort 라 복사할 LB 주소가 없습니다
      ingressEndpoint: { ip: [NODE_IP] }         # Ingress status 에 적을 주소. 비어 있으면 Argo CD 가 Ingress 를 계속 Progressing 으로 봅니다
  tlsStore:
    default:
      defaultCertificate: { secretName: wildcard-tls }         # 모든 Ingress 가 와일드카드 인증서를 씁니다 (tls: 불필요)
  accessLog: { enabled: true }
```
{: file="services/traefik/values.yaml" }

```yaml
apiVersion: v2
name: traefik
version: 0.1.0
dependencies:
  - name: traefik
    version: 41.6.0
    repository: https://traefik.github.io/charts
```
{: file="services/traefik/Chart.yaml" }

{% raw %}
```yaml
# 진입점(websecure) 기본 미들웨어. 접속자 IP(RemoteAddr)가 Cloudflare 대역이 아니면 403 을 돌려줍니다.
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: cloudflare-only
spec:
  ipAllowList:
    sourceRange:
{{- range .Values.cloudflareIPv4 }}
      - {{ . }}
{{- end }}
```
{: file="services/traefik/templates/middleware.yaml" }
{% endraw %}

```yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-tls
spec:
  secretName: wildcard-tls
  dnsNames: ["*.[DOMAIN]"]
  issuerRef: { kind: ClusterIssuer, name: letsencrypt }
```
{: file="services/traefik/templates/certificate.yaml" }

DDNS는 공인 IP를 `DOMAINS`에 적은 이름의 A 레코드(프록시 켬)에 반영합니다. 처음에는 노출할 서비스가 없으니 비워 두거나 5번에서 넣을 이름을 미리 적습니다.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns
spec:
  replicas: 1
  selector:
    matchLabels: { app: cloudflare-ddns }
  template:
    metadata:
      labels: { app: cloudflare-ddns }
    spec:
      containers:
        - name: cloudflare-ddns
          image: favonia/cloudflare-ddns:1.17.1
          env:
            - name: CLOUDFLARE_API_TOKEN
              valueFrom: { secretKeyRef: { name: cloudflare-api-token, key: token } }
            - { name: DOMAINS, value: "argocd.[DOMAIN],grafana.[DOMAIN]" }   # 서비스를 열 때 여기에 이름을 추가합니다
            - { name: PROXIED, value: "true" }       # 주황 구름: 집 IP 를 숨기고 Cloudflare 를 거칩니다
            - { name: IP6_PROVIDER, value: "none" }   # IPv4 만 씁니다
            - { name: UPDATE_CRON, value: "@every 5m" }
          resources:
            requests: { cpu: 10m, memory: 32Mi }
            limits: { memory: 64Mi }
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities: { drop: [ALL] }
```
{: file="services/cloudflare-ddns/deployment.yaml" }

> 와일드카드 레코드(`*.[DOMAIN]`)는 만들지 않습니다. 노드가 공유기 DHCP로 검색 도메인 `[DOMAIN]`을 받으면 파드의 `resolv.conf`에도 들어가고, `ndots:5` 때문에 `traefik.github.io` 같은 바깥 이름이 `traefik.github.io.[DOMAIN]`으로 먼저 조회됩니다. 와일드카드가 있으면 이 조회가 Cloudflare IP로 풀려 클러스터 안의 모든 외부 접속(Helm 저장소, Let's Encrypt, 이미지 pull)이 TLS 오류로 실패합니다. 실제로 겪은 일이며 트러블슈팅에 적었습니다.
{: .prompt-danger }

```bash
# 배포 확인 (컨트롤플레인에서)
kubectl get clusterissuer                        # READY True
kubectl -n traefik get certificate               # READY True (DNS-01 검증에 1~2분)
kubectl -n traefik get ds,svc                    # DaemonSet READY 노드 수, NodePort 31080/31443
kubectl -n cloudflare-ddns logs deploy/cloudflare-ddns | tail
```

- **확인:** ClusterIssuer와 Certificate가 `True`, Traefik DaemonSet이 모든 노드에서 Ready, DDNS 로그에 `Added a new A record for argocd.[DOMAIN]`이 보입니다.

## 4. 공유기 포트포워딩

WAN 443과 80을 Traefik NodePort로 넘깁니다. 공유기가 UPnP를 켜 두었다면 관리 화면 대신 스크립트로 겁니다. 매핑을 만드는 호스트만 내부 클라이언트로 허용하는 공유기가 많아 트래픽을 받을 노드에서 실행하며, 표준 라이브러리만 써서 노드에 패키지를 깔지 않습니다.

```bash
# 스크립트 내려받기
curl -fsSLO https://eu4ng.github.io/assets/scripts/kubernetes/router-portmap.py

# 컨트롤플레인에서 실행 (표준입력으로 스크립트를 넘깁니다)
ssh ubuntu@[CONTROL_PLANE_IP] python3 - < router-portmap.py
```

<details markdown="1">
<summary>router-portmap.py 전문</summary>

```python
#!/usr/bin/env python3
"""공유기(UPnP IGD)에 외부 노출용 포트포워딩을 겁니다. 표준 라이브러리만 씁니다.

    WAN 443 -> <이 호스트>:31443 (Traefik websecure NodePort)
    WAN 80  -> <이 호스트>:31080 (Traefik web NodePort)

MiniUPnPd 는 요청한 호스트만 내부 클라이언트로 허용하는 것이 보통이라, 트래픽을 받을 노드에서 실행합니다:

    ssh ubuntu@[CONTROL_PLANE_IP] python3 - < router-portmap.py            # 걸기 (이미 있으면 그대로 둠)
    ssh ubuntu@[CONTROL_PLANE_IP] python3 - --delete < router-portmap.py   # 지우기

임대 0(영구)으로 걸지만 공유기 재부팅·펌웨어 업데이트 뒤에는 사라질 수 있습니다. 외부 접속이 안 되면 먼저 다시 실행합니다.
공유기에서 UPnP 가 꺼져 있으면 관리 화면에서 위 두 개를 수동으로 겁니다.
"""
import re
import socket
import sys
import urllib.error
import urllib.request
import xml.etree.ElementTree as ET

MAPPINGS = [  # (외부 포트, 내부 포트, 설명)
    (443, 31443, "k8s traefik https"),
    (80, 31080, "k8s traefik http"),
]
PROTOCOL = "TCP"
LEASE = 0  # 0 = 영구
SSDP_ADDR = ("239.255.255.250", 1900)
DEVICE_NS = "{urn:schemas-upnp-org:device-1-0}"


def die(msg):
    sys.exit(f"[오류] {msg}")


def discover():
    """SSDP 로 IGD 의 설명 URL 을 찾습니다. 설명 URL 의 포트는 공유기가 재부팅될 때 바뀔 수 있어 매번 찾습니다."""
    msg = (
        "M-SEARCH * HTTP/1.1\r\nHOST: 239.255.255.250:1900\r\nMAN: \"ssdp:discover\"\r\nMX: 2\r\n"
        "ST: urn:schemas-upnp-org:device:InternetGatewayDevice:1\r\n\r\n"
    ).encode()
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM, socket.IPPROTO_UDP)
    s.settimeout(3)
    s.sendto(msg, SSDP_ADDR)
    try:
        while True:
            data, addr = s.recvfrom(4096)
            m = re.search(r"(?im)^location:\s*(\S+)", data.decode(errors="replace"))
            if m:
                return m.group(1), addr[0]
    except socket.timeout:
        die("공유기가 UPnP 에 응답하지 않습니다. 공유기 관리 화면에서 UPnP 를 켜거나 포트포워딩을 수동으로 거세요.")


def local_ip_toward(host):
    """공유기로 나가는 인터페이스의 주소 = 이 호스트가 내부 클라이언트로 등록할 IP."""
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    s.connect((host, 1900))
    return s.getsockname()[0]


class IGD:
    def __init__(self, location):
        self.base = re.match(r"(https?://[^/]+)", location).group(1)
        root = ET.fromstring(urllib.request.urlopen(location, timeout=5).read())
        name = root.find(f".//{DEVICE_NS}friendlyName")
        self.name = name.text if name is not None else "?"
        for svc in root.iter(f"{DEVICE_NS}service"):
            stype = svc.find(f"{DEVICE_NS}serviceType").text
            if "WANIPConnection" in stype or "WANPPPConnection" in stype:
                self.stype = stype
                self.control = svc.find(f"{DEVICE_NS}controlURL").text
                return
        die("IGD 에 WANIPConnection/WANPPPConnection 서비스가 없습니다.")

    def call(self, action, **args):
        """SOAP 호출. 성공하면 결과 dict, UPnP 오류면 (errorCode, errorDescription) 을 담은 UPnPError."""
        body = "".join(f"<{k}>{v}</{k}>" for k, v in args.items())
        envelope = (
            '<?xml version="1.0"?><s:Envelope xmlns:s="http://schemas.xmlsoap.org/soap/envelope/" '
            's:encodingStyle="http://schemas.xmlsoap.org/soap/encoding/"><s:Body>'
            f'<u:{action} xmlns:u="{self.stype}">{body}</u:{action}></s:Body></s:Envelope>'
        )
        req = urllib.request.Request(
            self.base + self.control, data=envelope.encode(),
            headers={"Content-Type": 'text/xml; charset="utf-8"', "SOAPAction": f'"{self.stype}#{action}"'},
        )
        try:
            text = urllib.request.urlopen(req, timeout=5).read().decode(errors="replace")
        except urllib.error.HTTPError as e:
            text = e.read().decode(errors="replace")
            code = re.search(r"<errorCode>(\d+)</errorCode>", text)
            desc = re.search(r"<errorDescription>(.*?)</errorDescription>", text)
            raise UPnPError(int(code.group(1)) if code else e.code, desc.group(1) if desc else text[:120])
        return {m.group(1): m.group(2) for m in re.finditer(r"<(New\w+)>(.*?)</\1>", text)}


class UPnPError(Exception):
    def __init__(self, code, desc):
        super().__init__(f"{code} {desc}")
        self.code = code


def list_mappings(igd):
    rows = []
    for i in range(256):
        try:
            r = igd.call("GetGenericPortMappingEntry", NewPortMappingIndex=i)
        except UPnPError:
            break
        rows.append(r)
    return rows


def main():
    delete = "--delete" in sys.argv[1:]
    location, router_ip = discover()
    igd = IGD(location)
    host = local_ip_toward(router_ip)
    print(f"공유기: {igd.name} ({router_ip}), 외부 IP: {igd.call('GetExternalIPAddress').get('NewExternalIPAddress')}")
    print(f"내부 호스트: {host}\n")

    for ext, internal, desc in MAPPINGS:
        key = dict(NewRemoteHost="", NewExternalPort=ext, NewProtocol=PROTOCOL)
        try:
            cur = igd.call("GetSpecificPortMappingEntry", **key)
        except UPnPError as e:
            cur = None if e.code in (714, 402, 501) else die(f"{PROTOCOL} {ext} 조회 실패: {e}")  # 714 = NoSuchEntryInArray

        if delete:
            if cur is None:
                print(f"{PROTOCOL} {ext}: 없음")
                continue
            igd.call("DeletePortMapping", **key)
            print(f"{PROTOCOL} {ext}: 삭제")
            continue

        if cur is not None:
            if cur.get("NewInternalClient") == host and cur.get("NewInternalPort") == str(internal):
                print(f"{PROTOCOL} {ext} -> {host}:{internal}: 이미 있음 (lease {cur.get('NewLeaseDuration')})")
                continue
            die(f"{PROTOCOL} {ext} 이 이미 {cur.get('NewInternalClient')}:{cur.get('NewInternalPort')} 로 걸려 있습니다. "
                "공유기 관리 화면에서 지우고 다시 실행하세요.")
        try:
            igd.call("AddPortMapping", **key, NewInternalPort=internal, NewInternalClient=host,
                     NewEnabled=1, NewPortMappingDescription=desc, NewLeaseDuration=LEASE)
        except UPnPError as e:
            die(f"{PROTOCOL} {ext} 추가 실패: {e}")
        print(f"{PROTOCOL} {ext} -> {host}:{internal}: 추가")

    print("\n현재 매핑:")
    for r in list_mappings(igd):
        print(f"  {r.get('NewProtocol')} {r.get('NewExternalPort'):>5} -> {r.get('NewInternalClient')}:{r.get('NewInternalPort'):<5} "
              f"lease={r.get('NewLeaseDuration')} '{r.get('NewPortMappingDescription')}'")


if __name__ == "__main__":
    main()
```
{: file="router-portmap.py" }

</details>

> 영구 임대(lease 0)로 걸지만 UPnP 규격은 공유기 재부팅 후까지 유지된다고 보장하지 않습니다. 외부 접속이 안 되면 이 스크립트를 먼저 다시 실행합니다. UPnP가 꺼져 있으면 관리 화면에서 WAN `443`/`80` → `[NODE_IP]:31443`/`31080`을 수동으로 겁니다.
{: .prompt-info }

- **확인:** `TCP 443 -> [NODE_IP]:31443: 추가`, `TCP 80 -> [NODE_IP]:31080: 추가`가 출력되고, 현재 매핑 목록에 두 줄이 `lease=0`으로 보입니다. 다시 실행하면 `이미 있음`으로 지나갑니다.

## 5. 서비스 노출

노출할 서비스마다 Ingress 하나를 추가하고, `services/cloudflare-ddns/deployment.yaml`의 `DOMAINS`에 이름을 넣습니다. `ingressClassName`과 `tls`는 적지 않습니다. Traefik이 기본 IngressClass이고 와일드카드 인증서를 자동으로 씁니다. Helm 스크립트로 설치한 Argo CD처럼 GitOps 밖의 앱도 Ingress만 폴더에 두면 됩니다.

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd
spec:
  rules:
    - host: argocd.[DOMAIN]
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: { name: argocd-server, port: { number: 80 } }
```
{: file="services/argocd/ingress.yaml" }

Helm 차트로 배포한 앱은 차트의 Ingress 값을 켭니다. Grafana는 리다이렉트와 링크가 외부 주소를 쓰도록 `root_url`도 함께 넣습니다.

```yaml
kube-prometheus-stack:
  grafana:
    ingress:
      enabled: true
      hosts: [grafana.[DOMAIN]]
    grafana.ini:
      server: { root_url: https://grafana.[DOMAIN] }
```
{: file="services/monitoring/values.yaml" }

- **확인:** push 후 `kubectl get ingress -A`에 새 Ingress가 보이고 ADDRESS 열에 `[NODE_IP]`가 적힙니다. DDNS 로그에 `Added a new A record for [서비스].[DOMAIN]`이 나옵니다.

## 6. 접속 확인

집 밖 네트워크(LTE, 회사망)에서 브라우저로 `https://argocd.[DOMAIN]`을 엽니다. 같은 검사를 명령으로 하면 다음과 같습니다.

```bash
# Cloudflare 를 거친 접속: 200 또는 앱 로그인 화면으로 302
curl -sI https://argocd.[DOMAIN] | head -1

# 프록시를 거치지 않고 집 IP 로 직접 찌르면 403 (ipAllowList)
curl -sk -o /dev/null -w "%{http_code}\n" --resolve argocd.[DOMAIN]:31443:[NODE_IP] https://argocd.[DOMAIN]:31443/

# HTTP 는 HTTPS 로 301
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" --resolve argocd.[DOMAIN]:31080:[NODE_IP] http://argocd.[DOMAIN]:31080/

# Traefik 이 제공하는 원본 인증서 (Let's Encrypt, *.[DOMAIN])
echo | openssl s_client -connect [NODE_IP]:31443 -servername argocd.[DOMAIN] 2>/dev/null | openssl x509 -noout -issuer -subject
```

- **확인:** Cloudflare 경유는 `200` 또는 `302`, 직접 접속은 `403`, HTTP는 `301`, 인증서 issuer는 `Let's Encrypt`입니다. Traefik 액세스 로그(`kubectl -n traefik logs ds/traefik`)의 첫 열이 Cloudflare 대역이 아니라 실제 접속자 IP면 `trustedIPs`도 동작하는 것입니다.

> 앞단에 로그인이 없으므로 접근 제어는 각 앱의 자체 로그인(Argo CD, Grafana, Headlamp 토큰)에 맡깁니다. 로그인이 없는 앱을 열 때는 Traefik forwardAuth와 Authelia 같은 인증 프록시를 먼저 둡니다. Cloudflare 무료 프록시는 요청 본문 100MB, 원본 첫 응답 100초 제한이 있어 큰 업로드나 오래 걸리는 API에는 맞지 않습니다.
{: .prompt-warning }

## 트러블슈팅

<details markdown="1">
<summary><code>remote error: tls: handshake failure</code> — Argo CD 가 Helm 저장소를 못 받고 Let's Encrypt 등록도 실패</summary>

```text
error building helm chart dependencies: failed to add helm repository https://traefik.github.io/charts:
... Get "https://traefik.github.io/charts/index.yaml": remote error: tls: handshake failure
Failed to register ACME account: Get "https://acme-v02.api.letsencrypt.org/directory": remote error: tls: handshake failure
```

- **원인:** 처음에는 와일드카드 `*.[DOMAIN]` 레코드로 시작했는데, 노드가 공유기 DHCP로 검색 도메인 `[DOMAIN]`을 받고 있었습니다. 파드 안에서 `getent hosts traefik.github.io`가 `traefik.github.io.[DOMAIN]`으로 풀려 Cloudflare IP를 돌려주었고, Cloudflare는 모르는 호스트명의 TLS 접속을 거부합니다. 호스트는 `ndots:1`이라 멀쩡했고 파드만 깨져 원인을 찾는 데 시간이 걸렸습니다.
- **해결:** 와일드카드 레코드를 지우고 DDNS가 서비스별 A 레코드만 만들게 바꿨습니다. 인증서는 와일드카드로 받아도 DNS-01 검증은 TXT 레코드만 쓰므로 문제가 없습니다.

</details>

<details markdown="1">
<summary><code>additional properties 'cloudflareIPv4' not allowed</code> — Traefik 차트 렌더링 실패</summary>

```text
Error: values don't meet the specifications of the schema(s) in the following chart(s):
traefik:
- at '': additional properties 'cloudflareIPv4' not allowed
```

- **원인:** Traefik 차트에 `values.schema.json`이 있어 `traefik:` 아래에 차트가 모르는 키를 두면 거부합니다. 미들웨어 템플릿에서 재사용하려고 대역 목록을 서브차트 값 안에 넣은 것이 문제였습니다.
- **해결:** 목록을 상위 차트 값(파일 루트)의 `cloudflareIPv4`로 옮기고 YAML 앵커로 `trustedIPs`에서 참조했습니다. 템플릿은 `.Values.cloudflareIPv4`를 읽습니다.

</details>

<details markdown="1">
<summary><code>Invalid API Token</code> — 토큰 검증 호출이 실패</summary>

```text
API 오류 GET /user/tokens/verify: 1000 Invalid API Token
```

- **원인:** 계정 소유 토큰(`cfat_`로 시작)은 `/user/tokens/verify`로 검증할 수 없습니다. 사용자 소유 토큰과 주소가 다릅니다.
- **해결:** 토큰이 `cfat_`로 시작하면 `/accounts/[ACCOUNT_ID]/tokens/verify`를 쓰도록 스크립트를 고쳤습니다. 계정 ID는 `GET /accounts`로 먼저 얻습니다.

</details>

<details markdown="1">
<summary>Argo CD 에서 Ingress 를 배포한 Application 이 계속 <code>Progressing</code></summary>

- **원인:** Argo CD는 `status.loadBalancer.ingress`가 비어 있는 Ingress를 아직 준비 중으로 봅니다. NodePort 서비스는 로드밸런서 주소가 없어 Traefik 기본 설정(`publishedService`)으로는 이 필드가 채워지지 않습니다.
- **해결:** `providers.kubernetesIngress.ingressEndpoint.ip`에 노드 주소를 적고 `publishedService`를 껐습니다. 적용 후 `kubectl get ingress -A`의 ADDRESS 열이 채워지고 Application이 Healthy가 됩니다.

</details>

<details markdown="1">
<summary><code>middleware "traefik-cloudflare-only@kubernetescrd" does not exist</code> — Traefik 시작 직후 로그</summary>

- **원인:** Traefik이 뜬 직후 Middleware 리소스가 아직 같은 sync에서 적용되기 전이라 진입점 기본 미들웨어를 찾지 못한 것입니다. 몇 초 뒤 CRD 프로바이더가 리소스를 읽으면 사라집니다.
- **해결:** 조치가 필요 없습니다. `kubectl -n traefik get middleware`에 `cloudflare-only`가 있고 이후 로그에 같은 오류가 없으면 정상입니다.

</details>

## 마무리

Cloudflare에는 DNS 레코드와 프록시 설정만 남고, 인증서·DNS 갱신·라우팅·차단 규칙은 저장소에, 포트포워딩은 스크립트에 있습니다. 서비스를 하나 더 열 때는 Ingress 파일과 DDNS 목록의 이름 한 줄이면 됩니다. 접속자가 Cloudflare를 거치므로 집 IP는 노출되지 않고, 프록시를 우회한 직접 접속은 Traefik이 403으로 막습니다. 앞단 인증이 없다는 점이 남은 한계라, 다음에는 Authelia로 로그인 없는 앱까지 안전하게 여는 방법을 다루려 합니다.

## 참고 자료

- [Cloudflare API token permissions](https://developers.cloudflare.com/fundamentals/api/reference/permissions/)
- [Cloudflare account owned tokens](https://developers.cloudflare.com/fundamentals/api/get-started/account-owned-tokens/)
- [Cloudflare IP Ranges](https://www.cloudflare.com/ips/)
- [cert-manager: Cloudflare DNS01 solver](https://cert-manager.io/docs/configuration/acme/dns01/cloudflare/)
- [Traefik Helm chart values](https://github.com/traefik/traefik-helm-chart/blob/master/traefik/values.yaml)
- [Traefik Kubernetes Ingress provider](https://doc.traefik.io/traefik/reference/install-configuration/providers/kubernetes/kubernetes-ingress/)
- [favonia/cloudflare-ddns](https://github.com/favonia/cloudflare-ddns)
- [UPnP InternetGatewayDevice WANIPConnection service](https://upnp.org/specs/gw/UPnP-gw-WANIPConnection-v1-Service.pdf)
