---
layout: post
title: 방법 템플릿
description: 검색 결과와 글 목록에 노출될 한 문장 요약을 적습니다.
author: Eu4ng
tags: []
---

<!--
[작성 규칙] 발행 전에 이 주석 블록과 본문의 안내 문장을 모두 삭제합니다.

대상 유형
- 설치, 설정, 연동, 환경 구축처럼 독자가 순서대로 따라 하는 글

front matter
- date, permalink, categories, last_modified_at, redirect_from 은 적지 않습니다.
  date 는 발행할 때, permalink 는 scripts/add_permalinks.py 가, 나머지는 _plugins 가 채웁니다.
- title 은 "~하는 방법" 또는 "~하기" 형태로 결과가 드러나게 씁니다.
- tags 는 소문자와 하이픈만 씁니다. 예: gitlab, ci-cd, unreal-engine

파일 위치
- _posts/카테고리/하위카테고리/YYYY-MM-DD-슬러그.md
- 폴더 이름이 곧 카테고리입니다. 기존 폴더와 대소문자, 띄어쓰기를 똑같이 맞춥니다.
- 슬러그는 제목을 소문자로 바꾸고 공백을 하이픈으로 바꿉니다. 확장자 앞에 공백이 없어야 합니다.

제목 구조
- 본문은 H2 로 시작하고 H2 와 H3 만 씁니다.
- 단계 제목은 "### 1. 제목" 형식이며, 번호가 중복되거나 건너뛰지 않았는지 마지막에 점검합니다.
- (선택) 표시가 붙은 섹션은 쓸 내용이 없으면 통째로 지웁니다. 제목의 "(선택)" 글자도 지웁니다.

문체와 강조
- 본문 문장은 합니다체, 목록 항목은 개조식으로 씁니다.
- 굵은 글씨는 UI 라벨과 핵심 용어의 첫 등장에만 씁니다. 제품 이름마다 굵게 하지 않습니다.
- UI 경로는 **설정** > **일반** > **고급** 형식으로 씁니다.
- 파일 경로와 명령어, 설정 키는 인라인 코드로 씁니다.

코드
- 펜스 언어: 실행 명령은 bash, 설정은 yaml 또는 json, C++ 는 cpp, 그 외 dockerfile, ini, text
- 파일 내용인 코드 블록에는 바로 아래 줄에 파일 라벨을 붙입니다. 중괄호 안쪽에 공백을 둡니다.
- 명령 블록은 주석으로 단계를 구분하고, 한 블록에는 한 가지 목적의 명령만 담습니다.
- 자리표시자는 [UPPER_CASE] 로 통일합니다. 실제 토큰, 조직 이름, 내부 주소는 절대 쓰지 않습니다.
- 본문에 이중 중괄호나 중괄호 퍼센트 표기가 들어가면 해당 코드 블록을 Liquid raw 태그로 감쌉니다.

프롬프트 블록
- prompt-info: 선행 조건, 건너뛰어도 되는 경우, 관련 글 안내
- prompt-tip: 더 편한 방법, 알아 두면 좋은 점
- prompt-warning: 실수하기 쉬운 부분, 버전에 따라 달라지는 동작
- prompt-danger: 데이터 손실, 보안, 비용, 라이선스 문제
- 한 섹션에 두 개 이상 쓰지 않습니다.

이미지
- /assets/img/post/ 아래에 두고, 대체 텍스트를 구체적으로 씁니다.
- 바로 아래 줄에 기울임 글씨로 캡션을 씁니다.

참고 자료
- "- [제목](URL)" 목록으로 씁니다. 공식 문서를 먼저, 블로그를 뒤에 둡니다.
- 제목에 세로선이 있으면 역슬래시로 이스케이프합니다.
-->

## 개요

무엇을 하는 글이고, 끝까지 따라 하면 무엇이 완성되는지 한 문장으로 적습니다.
예: GitLab Runner가 GHCR의 비공개 이미지를 가져오도록 인증을 설정하는 과정을 정리한 문서입니다.

## 서론 (선택)

이 글을 쓰게 된 배경과, 여러 방법 중 이 방법을 고른 이유를 두세 문장으로 적습니다.
공식 문서의 방식과 다르게 진행했다면 무엇이 다른지 여기서 밝힙니다.

## 사전 준비

> 이미 준비되어 있는 경우 건너뛰셔도 됩니다.
{: .prompt-info }

### 환경

| 항목 | 버전 |
| :--- | :--- |
| 운영체제 | `예: Windows 11 24H2` |
| 핵심 도구 | `예: Docker Desktop 4.40` |
| 작성 기준일 | `YYYY-MM-DD` |

### 준비물

- 미리 설치되어 있어야 하는 도구
- 필요한 계정과 권한
- 먼저 읽어야 하는 글이 있다면 [글 제목](/posts/N/) 링크

## 방법

전체 흐름을 한두 문장으로 먼저 안내합니다. 단계가 네 개 이상이면 번호 목록으로 요약합니다.

### 1. 단계 제목

이 단계에서 무엇을, 왜 하는지 한 문장으로 적습니다.

1. **메뉴** > **하위 메뉴**로 이동
2. **버튼 이름** 클릭
3. 아래 값을 입력
   - **항목 이름**: `값`

```yaml
services:
  app:
    image: [IMAGE_NAME]
```
{: file="docker-compose.yml" }

- **확인:** 이 단계가 성공했을 때 화면이나 출력에 무엇이 보이는지 적습니다.

### 2. 단계 제목

```bash
# 무엇을 하는 명령인지
command --option [VALUE]
```

> 실수하기 쉬운 부분이나 버전에 따라 달라지는 동작을 적습니다.
{: .prompt-warning }

- **확인:** 기대하는 출력이나 상태를 적습니다.

## 트러블슈팅 (선택)

### 증상을 한 줄로 요약한 제목

```text
실제로 출력된 오류 메시지 전문
```

- **원인:** 왜 발생했는지 적습니다. 확인하지 못한 추정이면 추정이라고 밝힙니다.
- **해결:** 무엇을 어떻게 바꿨는지 적습니다.

## 마무리

개요에서 예고한 내용을 받아 무엇을 완성했는지 한두 문장으로 정리합니다.
이 방법의 한계, 직접 써 본 소감, 이어서 다룰 내용이 있다면 덧붙입니다.

## 참고 자료

- [공식 문서 제목](URL)
- [블로그 글 제목](URL)
